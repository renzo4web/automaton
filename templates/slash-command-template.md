---
name: "/command-name"
description: "What this command does"
---

# Context

This command runs AFTER the user made changes.
First, run `git status` and `git diff` to see what was changed.
Focus your work on the modified/untracked files.

# Behavior

[What to do with the changes]

# Arguments example

If the command accepts arguments, reference them explicitly so the prompt knows what to substitute, e.g. `Create a new component named $ARGUMENTS` or `Generate a file called $1 inside $2 using the content $3`.

# Output

[What to return]
